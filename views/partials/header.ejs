<!DOCTYPE html>
<html>
<head>
    <title>Time Logs</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
	<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.13/css/all.css">
    <link href="/css/style.css" rel="stylesheet" />
</head>
<body>
  <header class="mb-5">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
	  <div class="container">
      <a class="navbar-brand" href="/">BOS</a>
      <span class="navbar-text">
        The virtual accountant
      </span>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        
        <ul class="navbar-nav">
		  <li class="nav-item dropdown">
			  <a class="nav-link dropdown-toggle" href="/users/"
				id="navbarDropdownResources" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
				Resources
			  </a>
			  <div class="dropdown-menu" aria-labelledby="navbarDropdownResources">
				<a class="dropdown-item" href="/pivot-example" >Pivot Table Examples</a>
				<a class="dropdown-item" href="/random-data">Random Sales Data</a>

			  </div>
          </li>
		  <li class="nav-item">
			<a class="nav-link" href="/leaderboard">
				Leaderboard
				<span style="color:gold;">
					<i class="fas fa-star"></i>
				</span>
			</a>
		  </li>
          <% if(currentUser && currentUser.isAdmin) { %>
              <li class="nav-item">
                <a class="nav-link" href="/dashboard?date=<%= new Date().toLocaleDateString() %>">
                  Admin dashboard
                </a>
              </li>
              <li class="nav-item"><a class="nav-link" href="/users/new" >Create a new user</a></li>
          <% } %>
          <% if(currentUser) { %> 
            <li class="nav-item">
              <a class="nav-link" href="/users/<%= currentUser.username %>">Profile</a>
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="/users/<%= currentUser.username %>"
                id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Options
              </a>
              <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                <a class="dropdown-item" href="/users/change" >Change password</a>
                <a class="dropdown-item" href="/logout">Logout</a>

              </div>
            </li>
          <% }  else { %>
              <li class="nav-item"><a class="nav-link" href="/">Sign In</a></li>
          <% } %>%>
        </ul>
      </div>
      </div>
    </nav>
  </header>

<div class="container">
    <% if (error.toString()) { %>
    <div class="alert alert-danger">
        <%= error %>
    </div>
    <% } %>
    <% if (success.toString()) { %>
    <div class="alert alert-success">
        <%= success %>
    </div>
    <% } %>
</div>